#!/usr/bin/env ruby

input = ARGV[0]
output_path = ARGV[1]

File.open(output_path, 'w') do |output|
  converter = Encoding::Converter.new("US-ASCII", "utf-8", invalid: :replace)
  utf8 = converter.convert(File.read(input))
  output.write(utf8)
end
exit 0
