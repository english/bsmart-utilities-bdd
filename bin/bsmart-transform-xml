#!/usr/bin/env ruby
require 'iconv'

input = ARGV[0]
output_path = ARGV[1]

File.open(output_path, 'w') do |output|
  if utf8 = Iconv.conv("utf8//ignore//translit", "us-ascii", File.read(input))
    output.write(utf8)
  else
    exit 1
  end
end
exit 0
